'use strict';

// The blog template seeds example About/Article/Global data on first boot via
// ./bootstrap. We removed those example content types, so that seed would throw.
// Bootstrap is now a no-op — real content is created in the Strapi admin.
module.exports = {
  register(/* { strapi } */) {},
  bootstrap(/* { strapi } */) {},
};
